<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width" />
  <title>{{title}}</title>

  <!-- Your shared color variables -->
  <link rel="stylesheet" href="/css/colors.css">

  <!-- Page-specific styles to mirror your site -->
  <style>
    @import url('https://fonts.googleapis.com/css?family=Lexend Deca');
    @import url('https://fonts.googleapis.com/css?family=Inter');

    :root {
      /* falls back just in case, real values should come from /css/colors.css */
      --one: #6a5acd;
      --two: #b39ddb;
      --three: #ffffff;
    }

    body {
      display: flex;
      justify-content: center;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
      background: linear-gradient(to left, var(--one), var(--two), var(--three));
      font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
    }

    .container {
      background-color: var(--two);
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      width: 700px;
      height: 500px;
      margin: 90px auto 0;        /* centered horizontally */
      box-shadow: 0 0 10px rgba(0,0,0,0.5);
      border-radius: 10px;
    }

    .title {
      font-family: 'Lexend Deca', sans-serif;
      color: var(--three);
      font-size: 22px;
      margin: 0 0 10px 0;
      letter-spacing: .5px;
      text-align: center;
    }

    .subtitle {
      color: var(--three);
      opacity: .9;
      font-size: 14px;
      margin: 0 0 16px 0;
      text-align: center;
      max-width: 460px;
      line-height: 1.4;
    }

    .register-info {
      width: 400px;
      font-family: 'Inter', sans-serif;
      font-size: 16px;
      font-weight: 400;
      color: var(--three);
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    label {
      font-size: 14px;
      margin-top: 6px;
    }

    input[type="password"] {
      width: 100%;
      padding: 10px 12px;
      box-sizing: border-box;
      border-radius: 6px;
      border: 2px solid var(--one);
      background: var(--three);
      color: #111;
      font-size: 14px;
      outline: none;
    }
    input[type="password"]:focus {
      background: #cfc6e8; /* matches your focus style */
    }

    .requirements {
      font-size: 12px;
      color: #172554; /* deep indigo for contrast, can tweak */
      background: rgba(255,255,255,0.55);
      border-radius: 8px;
      padding: 8px 10px;
      margin-top: 4px;
      line-height: 1.35;
    }

    .actions {
      display: flex;
      justify-content: flex-end;
      gap: 10px;
      margin-top: 16px;
    }

    .btn {
      cursor: pointer;
      border: none;
      padding: 10px 15px;
      font-size: 14px;
      border-radius: 8px;
      font-weight: 500;
    }
    .btn-primary {
      background-color: var(--one);
      color: var(--three);
    }
    .btn-plain {
      background: transparent;
      color: var(--three);
      text-decoration: underline;
    }

    .msg {
      margin-top: 10px;
      font-size: 14px;
      min-height: 18px; /* keeps space reserved */
    }

    @media (max-width: 860px) {
      .container {
        width: calc(100% - 32px);
        height: auto;
        padding: 24px 16px;
      }
      .register-info {
        width: 100%;
      }
    }
  </style>

  <!-- Your reset page logic -->
  <script defer src="/js/reset-password.js"></script>
</head>
<body>
  <div class="container" data-token="{{token}}">
    <h1 class="title">Reset your password</h1>
    <p class="subtitle">Enter and confirm a new password for your account.</p>

    <div class="register-info">
      <label for="newPassword">New password</label>
      <input type="password" id="newPassword" placeholder="Enter new password" autocomplete="new-password" />

      <label for="confirmPassword">Confirm new password</label>
      <input type="password" id="confirmPassword" placeholder="Re-enter new password" autocomplete="new-password" />

      <div class="requirements">
        Must be at least 8 characters and include uppercase, lowercase, a number, and a symbol (@#$%^&+=).
      </div>

      <div class="actions">
        <a class="btn btn-plain" href="/">Cancel</a>
        <button id="submitReset" class="btn btn-primary" type="button">Reset Password</button>
      </div>

      <div id="msg" class="msg"></div>
    </div>
  </div>
</body>
</html>
